import ActionTypes from './ActionTypes';
import Dispatcher from './Dispatcher';

const Actions = {
    /*
    exampleAction(parameter){
        type:ActionTypes.ACTION_NAME,
        text,
    };
    */
};

export default Actions;